# backend/app/services/embedding_service.py

def generate_embeddings(chunks):
    """
    Placeholder for future Gemini embedding generation.
    """

    embeddings = []

    for chunk in chunks:
        embeddings.append({
            "text": chunk,
            "embedding": []
        })
